---
title: "Build Changes"
description: "Files that require a container restart when modified in WordPressify."
date: "2026-03-18"
category: "Customization"
categoryOrder: 1
order: 4
---

# Build Changes

Changes to the following files require a container restart:

| File | Description |
|------|-------------|
| `config/` | Nginx & PHP configurations |
| `.babelrc` | Babel configuration |
| `.dockerignore` | Docker ignore file |
| `.env` | Environment variables |
| `docker-compose.yml` | Docker Compose configuration |
| `Dockerfile-nodejs` | Node.js Dockerfile |
| `Dockerfile-wordpress` | WordPress Dockerfile |
| `gulpfile.js` | Gulp configuration |
| `package.json` | Node.js packages |

## Restart Instructions

Stop the running process with `CTRL/CMD+C`, then rebuild:

<Tabs>
  <TabContent title="npm">
    ```bash
    npm run build
    ```
  </TabContent>
  <TabContent title="Docker">
    If Node.js is not installed locally:

    ```bash
    docker compose build
    ```
  </TabContent>
</Tabs>
